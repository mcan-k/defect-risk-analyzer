# =============================================================================
# Predictive Defect Analysis Engine — Core Dependencies
# =============================================================================
# Install: pip install -r requirements.txt
#
# This is everything the dashboard and the CI analyzer need. The FastAPI
# server is optional — it only serves the Jira webhook and external
# integrations — and its dependencies live in requirements-webhook.txt.
# =============================================================================

# --- Frontend ---
streamlit==1.63.0
plotly==5.24.1

# --- LLM Providers (BYOK) ---
groq==1.7.0
openai==3.13.0

# --- Vector Database ---
chromadb==0.6.3

# --- Jira Integration ---
requests==2.34.2

# --- Environment & Config ---
python-dotenv==1.2.3

# --- Data Processing ---
numpy==2.4.6
pandas==2.2.3
